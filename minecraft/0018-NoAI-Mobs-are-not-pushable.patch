From ca83c94c167ff5c432fda28003b8d6e69fd7432e Mon Sep 17 00:00:00 2001
From: Brokkonaut <hannos17@gmx.de>
Date: Thu, 9 Jan 2025 09:01:31 +0100
Subject: [PATCH] NoAI Mobs are not pushable

---
 net/minecraft/world/entity/Entity.java | 22 ++++++++++++++++++++--
 1 file changed, 20 insertions(+), 2 deletions(-)

diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index ad432295..1d896a64 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -2288,6 +2288,12 @@ public abstract class Entity implements SyncedDataHolder, DebugValueSource, Name
     public void playerTouch(Player player) {
     }
 
+    // Brokko start - pushable
+    private boolean isRealPushable() {
+        return !(this instanceof Mob) || !((Mob) this).isNoAi();
+    }
+    // Brokko end
+
     public void push(Entity entity) {
         if (!this.isPassengerOfSameVehicle(entity)) {
             if (!entity.noPhysics && !this.noPhysics) {
@@ -2309,11 +2315,23 @@ public abstract class Entity implements SyncedDataHolder, DebugValueSource, Name
                     d *= 0.05F;
                     d1 *= 0.05F;
                     if (!this.isVehicle() && this.isPushable()) {
-                        this.push(-d, 0.0, -d1);
+                        //Brokko start - isRealPushable
+                        if (this.isRealPushable()) {
+                            this.push(-d, 0.0, -d1);
+                        } else {
+                            this.needsSync = true;
+                        }
+                        //Brokko end
                     }
 
                     if (!entity.isVehicle() && entity.isPushable()) {
-                        entity.push(d, 0.0, d1);
+                        //Brokko start - isRealPushable
+                        if (entity.isRealPushable()) {
+                            entity.push(d, 0.0D, d1);
+                        } else {
+                            entity.needsSync = true;
+                        }
+                        //Brokko end
                     }
                 }
             }
-- 
2.50.1.windows.1

